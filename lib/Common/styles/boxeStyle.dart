import 'package:flutter/material.dart';

import '../Const/const.dart';

final borderContainer = BoxDecoration(
  border: Border.all(
    width: 1,
    color: darkGrayColor,
  ),
  borderRadius: BorderRadius.circular(20),
);

final filledContainer = BoxDecoration(
  color: secondaryColor,
  borderRadius: BorderRadius.circular(20),
);
